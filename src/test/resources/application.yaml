wiremock.server.port: 8090
lookup:
  colour:
    endpoint: http://localhost:${wiremock.server.port}/colour/%s
  size:
    endpoint: http://localhost:${wiremock.server.port}/size/%s
  description:
    endpoint: http://localhost:${wiremock.server.port}/description/%s